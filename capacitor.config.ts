import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.wyomingsoftware.biblereader",
  appName: "Bible Reader",
  webDir: "dist",
};

export default config;
