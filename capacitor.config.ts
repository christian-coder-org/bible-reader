import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "org.christiancoder.biblereader",
  appName: "Bible Reader",
  webDir: "dist",
};

export default config;
